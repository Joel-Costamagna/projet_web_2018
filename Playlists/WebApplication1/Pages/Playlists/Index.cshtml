@page
@model Playlist.Pages.Playlists.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.PlaylistModel[0].nom)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.PlaylistModel[0].description)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.PlaylistModel[0].isPublic)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.PlaylistModel) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.isPublic)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
